Audit logs are now recorded for the privileged actions the backend performs — bans, kicks, member nick/timeout/role updates, role and channel edits/deletes, member voice moves, and scheduled-event CRUD. Read them through `guild.audit_logs()` (with `user`/`action`/`before`/`after` filtering), the `on_audit_log_entry_create` event, or `env.guild.audit_log()`. Reasons passed via discord.py's `reason=` are captured (and now also stored on the ban record itself).
