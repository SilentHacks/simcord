Invites, custom emojis and stickers are supported: create/list/fetch/delete invites (`INVITE_CREATE`/`INVITE_DELETE`) and guild expression CRUD (`GUILD_EMOJIS_UPDATE`/`GUILD_STICKERS_UPDATE`), with builder helpers `GuildHandle.create_emoji` / `create_sticker` for setup.
