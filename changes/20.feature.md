Auto-moderation rules can be created, edited and deleted, and keyword rules are evaluated on message send: a matching `block_message` action drops the message and fires `AUTO_MODERATION_ACTION_EXECUTION`, honoring exempt roles and channels.
