Polls are supported end to end: a bot sends `discord.Poll`, users cast and retract votes with `MemberActor.vote` / `remove_vote` (firing `MESSAGE_POLL_VOTE_ADD`/`REMOVE`), poll answer voters are fetchable, and polls finalize either via `Message.end_poll()` or when `env.advance_time` passes their deadline.
