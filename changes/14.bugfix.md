Passing the wrong handle kind to a typed slash-command option (e.g. a `RoleHandle` to a `USER` option) now fails with a clear `SetupError` at the call site, instead of resolving into the wrong bucket and failing deep inside discord.py.
