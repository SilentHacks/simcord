New channel kinds can be created from builders: voice, stage, category and forum channels (`GuildHandle.create_voice_channel`, `create_stage_channel`, `create_category`, `create_forum_channel`), and `create_text_channel` accepts a `category=`.
